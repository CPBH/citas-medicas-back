json.extract! historium, :id, :numeroDocumento,:enfermedades, :string, :medicamentos, :cirugiias, :antecedentes, :resultados, :created_at, :updated_at
json.url historium_url(historium, format: :json)
